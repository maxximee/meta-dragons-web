pragma solidity ^0.8.4;

import "./dragonminting.sol";

contract DragonMerging is DragonMinting{
    function mergeDragons(uint _dragonA, uint _dragonB_) public returns (uint mergedDragon)  {
        return 0;
    }
}
